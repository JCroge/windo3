# 待解决事项

更新日期：2026-05-27  
当前唯一待办入口：[`docs/to-do-list.md`](to-do-list.md)

此文件保留用于兼容旧链接。当前状态、已关闭事项、阻断 live 扩容的问题和后续 P2 优化统一维护在 `docs/to-do-list.md`，避免多份待办列表分叉。

当前结论：

- 自动化整改验证通过：`618 passed / 4 deselected / 1 warning`（含 partial TP lifecycle 32 case + Long Entry Position Guard 23 case + R:R Floor Policy 20 case + OKX posMode 38 case）。
- OKX mock 语义验收通过：10 case PASS（含 posMode close 矩阵 + 拒单状态复核）。
- 分批止盈生命周期收敛阶段 1+2+3 完成（2026-05-27）：`_build_okx_attach_algo` 不再带 TP，`_replace_protective_sl` 单一入口，`_migrate_okx_algos_for_symbol` 重启对账。
- Long Entry Position Guard 上线（2026-05-26）：`Judge._check_entry_position_policy` 单一函数收敛主路径与三条 deferred 路径。
- R:R Floor Policy 修复完成（2026-05-26）：`Judge._select_rr_floor` 单一函数收敛主路径与 deferred 路径。
- **OKX 真实 testnet 语义验收完成**（2026-05-27）：T0/T1/T4/T5/T6/T8/T9 PASS，T2/T3 SKIP（账户为 long_short_mode），T7 SKIP（mock_only 已通过）。关键 bug `_cancel_protective_sl` 50002 已修。**Live 扩容前置阻断已解除**，下一步小额 24h 灰度观察 segmented metrics。

