# 待解决事项

更新日期：2026-05-26  
当前唯一待办入口：[`docs/to-do-list.md`](to-do-list.md)

此文件保留用于兼容旧链接。当前状态、已关闭事项、阻断 live 扩容的问题和后续 P2 优化统一维护在 `docs/to-do-list.md`，避免多份待办列表分叉。

当前结论：

- 自动化整改验证通过：`551 passed / 4 deselected / 1 warning`（含 R:R Floor Policy 20 case + OKX posMode 38 case）。
- OKX mock 语义验收通过：10 case PASS（含 posMode close 矩阵 + 拒单状态复核）。
- R:R Floor Policy 修复完成（2026-05-26）：`Judge._select_rr_floor` 单一函数收敛主路径与 deferred 路径，新增 `long_aligned_low_rr` 策略允许 mixed/choppy 趋势强一致多头按 1.30 floor 入场。
- OKX posMode 执行兼容代码完成（CODE-COMPLETE / WAITING-TESTNET），等真实 demo/testnet T0-T9 证据。
- OKX 真实 testnet 语义验收未执行，仍阻断 live 扩容。
